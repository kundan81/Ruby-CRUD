<!-- Method that needs to be consumed into layout.erb ,edited by sibu-->

<table border="10" width="1000px" >
<th align="left"><i>Name of the intern--Along with their Trainers</i></th></br>
<tr>
<td>
	<ul id="intern-list" class = "unstyled">
		
		<% @interns.each do |item| %>
	
		<li id="<%= item[:id]%>">
			
			<%= item[:done] ? "<del>#{item[:intern_name]}</del>" : item[:intern_name] %> 	
			&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
			&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
			
		<b>Trainer - </b> <b><%=item[:done] ?"<del>#{item[:working_with]}</del>"</b> : item[:working_with]%>
	<!--	<b>Skills ></b> <%=item[:done] ?"<del>#{item[:skills_Added]}</del>" : item[:skills_Added]%>	
		-->
		<!--<b>Image </b><%= item[:done]? "<del>#{item.image_add} </del>" :item[:image_add] %> -->
		
	<div align="left"><img src= <%= item[:image_add] %> width ="50px" height ="50px alt="<%= item[:id] %>"/></div>			
				<span class = "pull-right">
				<a href="/edit/<%= item[:id] %>" class="btn btn-danger"> Edit</a>
				<a href="/delete/<%= item[:id] %>" class="btn btn-danger"> Delete</a> 
			</span>	
		</li>		

			
		<% end %>

</td>
</tr>
</table>
					

</ul>
<table padding= 100px >
	<tr> <a href="/add_intern" class="btn btn-primary">Add Intern</a>
	    &nbsp;&nbsp;
	<tr> <a href="/print" class="btn btn-primary">&nbsp;&nbsp;&nbsp;&nbsp;Print &nbsp;&nbsp;&nbsp;&nbsp;</a></tr>

</table>

